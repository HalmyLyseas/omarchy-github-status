// BarWidget.qml -- STUB for stage S1 (scaffold + model). Stage S3 replaces
// this file with the real bar icon + count badge + eager-Loader Panel.qml
// hosting pattern described in exchange/03-shell-api.md §4 (the canonical
// shape used by Ristretto and the first-party weather/clock widgets).
//
// This stub exists only so `entryPoints.barWidget` resolves to a real file
// that satisfies `omarchy plugin validate` (entry-point existence check).
import QtQuick

Item {
    id: root
}
