// Service.qml -- STUB for stage S1 (scaffold + model). Stage S2 replaces this
// file with the full data-layer implementation against the Service public
// API contract in exchange/06-design.md ("Service public API"): status,
// lastSyncMs, rateLimitedUntil, busy, notifications, unreadCount,
// reviewRequests, openPRs, repos, hasAttention, refresh(), openUrl(url).
//
// This stub exists only so `entryPoints.service` resolves to a real file
// that satisfies `omarchy plugin validate` (entry-point existence check).
import QtQuick

Item {
    id: root
}
