# Local scoping/handoff notes, not part of the plugin: this repository
# doubles as the installed plugin folder, so anything tracked here is
# copied onto every user's machine.
/exchange/

*.bak
node_modules/

# editor / OS scratch
*~
*.swp
.DS_Store
